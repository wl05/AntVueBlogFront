// import { generateAuthCode } from '@/api/user'

const signup = {
  state: {},
  mutations: {
    // SAVE_ARTICLE_DETAIL: (state, detail) => {
    //   state.detail = detail
    // },
  },
  actions: {
    // GENERATE_AUTH_CODE: ({commit, dispatch, state}) => {
    //   return new Promise((resolve, reject) => {
    //     generateAuthCode()
    //     .then(response => {
    //       const data = response.data
    //       if (data.code) {
    //         reject(response)
    //       } else {
    //         resolve(response)
    //       }
    //     })
    //     .catch(error => {
    //       reject(error)
    //     })
    //   })
    // },
  },
}

export default signup
